## Release 签名配置（绝不入库真实值）
## 复制本文件为 keystore.properties，填入你自己的 release keystore 信息。
##
## 生成你自己的 keystore：
##   keytool -genkey -v -keystore my-release.keystore -alias my-key \
##     -keyalg RSA -keysize 2048 -validity 10000
##
## keystore.properties 与 *.jks / *.keystore 均在 .gitignore，不会被提交。
## build.gradle.kts 中的 signingConfigs.release 会从此文件读取上述四个字段。
storeFile=/path/to/your-release.keystore
storePassword=YOUR_STORE_PASSWORD
keyAlias=YOUR_KEY_ALIAS
keyPassword=YOUR_KEY_PASSWORD
