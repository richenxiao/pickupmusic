# PickUpMusic v1.2.0 真机验证清单

APK：`app/build/outputs/apk/release/app-release.apk`（versionCode 10200，同官方签名，覆盖 v1.1.0 保留数据）
分支：`feature/v1.2.0-optimization`（14 个 commit，未打 tag/发版）

安装：`adb install -r app/build/outputs/apk/release/app-release.apk`

---

## 阶段一：R8 体积优化（关键验证）

**体积**：71MB → 26.25MB（-63%）。R8 风险点已用 instrumented test 验证（Kuromoji 振假名、Gson 解析、DataStore 不崩）。真机抽样确认这些 UI 交互没被 R8 破坏：

- [ ] **振假名**：放一首日文歌词的歌 → 进「歌词本」全屏页 → 汉字上方有假名、读音正确（如 音楽→おんがく）【R8 最高风险点，已 instrumented test 验证逻辑，真机确认渲染】
- [ ] **专辑封面/歌手数据**：专辑列表封面正常加载（iTunes）；歌手页头像/信息正常
- [ ] **App 启动**：冷启动无崩溃、无 ANR
- [ ] **拖拽排序**：播放队列/歌单拖拽换序正常
- [ ] **设备切换**：系统 Output Switcher 弹出、切换音频输出正常
- [ ] **歌手合并**：合并歌手操作正常
- [ ] **播放队列右滑移除**：队列项右滑删除正常
- [ ] **播放速度**：速度面板档位/自定义/复古模式正常
- [ ] **隐藏此曲**：隐藏后歌曲从列表消失

**有崩**：logcat 抓崩溃栈贴给我，mapping.txt 在 `app/build/outputs/mapping/release/mapping.txt`，我能反混淆定位。

---

## 阶段二：加载速度（冷启动提速）

**已验证**（模拟器）：`incremental HIT` 跳过全量 scan、磁盘缓存代码不崩。真机大库测提速数字：

- [ ] 第一次冷启动（写缓存）：正常耗时，记录时间
- [ ] 第二次冷启动（增量命中）：应明显更快（跳过全量 MediaStore query + 封面不再重下网络）
- [ ] 抓 logcat 的 `ScanTrace` 行：`incremental HIT sig=... tracks=... (skipped full scan)` = 命中；`incremental MISS full scan` = 全量
- [ ] 新增/删除音乐文件后下次冷启动能正确识别（签名变→全量）

把两次冷启动时间告诉我（或确认第二次明显更快）。

---

## 阶段三：数据备份/恢复

**已验证**（instrumented test）：export JSON → 清表 → import 恢复修正回来。真机实测：

- [ ] 设置页 →「备份我的修正数据」→「备份」→ 选保存位置 → 生成 JSON
- [ ] 提示「已备份全部修正数据」
- [ ] 清 App 数据（或卸载重装）后 →「恢复」→ 选刚才 JSON
- [ ] 提示「恢复完成：专辑迁移X / 专辑修正X / 曲目修正X / ...」
- [ ] 之前的修正（专辑名/封面/隐藏/歌手合并/振假名注音）都回来

---

## 阶段四：本会话新增 feature（真机确认）

- [ ] **#7 音乐库 fast-scroll**：音乐库列表右侧滑动条，大库拖 thumb 快速跳转；内容不超视口时隐藏。
- [ ] **#4 首页左缘右滑拉侧边栏**：主内容左缘右滑开侧边栏（≥48dp）；中屏右滑不开；back/点 scrim 关；竖向滚动不受影响。
- [ ] **#8 搜索分词跨字段**：搜「周杰伦-黑色幽默」能命中本作（歌手×标题），不再整串漏匹配；单字段多 token 仍准。
- [ ] **#9 设备切换回本机后图标刷新**：系统 Output Switcher 切回本机扬声器后，播放页设备图标应刷新为扬声器（延迟 500ms 重读）。
- [ ] **振假名批量修正 + 同字速选**：长按歌词行→选词→「修正读法」对话框应有「同字读法速选」chip（点一下填入）+「同时应用本曲全部（共 N 处）」勾选；批量保存后同字全部生效。
- [ ] **振假名注音段换行不拆字**：长 surface（如「二人」）在歌词换行处应整段落到新行、注音跟随，不再出现「二人」拆两行、ふたり 只标在 二 上。
- [x] **#5 搜索结果按专辑折叠**（真机已验：搜 dawn → Dawn FM 17 首命中折叠成 1 张专辑卡）。
- [ ] **#3 音乐库「合集」分类**：同文件夹多歌手单曲应判为「合集」（不属任一歌手）；音乐库专辑列表显示「合集」标签；歌手页不应出现合集。真正专辑（即便有 feat.）不被误判。

## 阶段五：余下待做（视觉，需真机重连迭代）

- [ ] **#1a 歌词本动画优化**：句间过渡卡顿/帧率低。视觉，需真机调。
- [ ] **#1b 振假名按钮「あ」圆圈对齐**：LyricsScreen.kt:337-350。视觉微调，需真机看 baseline 后调字形偏移（疑似 includeFontPadding）。
- [ ] **#6 歌手页折叠视差重设计（大重构）**：写真大图背景+歌手名+操作栏+歌曲/专辑栏；向下滚上层盖写真（视差）；写真复用 ArtistAvatarFetcher+持久化；Compose nested scroll 折叠头部。

## 验完反馈

任意一项异常 → logcat/crash_logs 贴给我。全部 OK → 回复「v1.2.0 真机验证通过」，我打 tag v1.2.0 + 发 GitHub Release + 合并 develop。
